# Jenkins CI/CD

## Requirement: Provide Basic Jenkins Pipeline

The project SHALL provide a Jenkins pipeline skeleton covering checkout, backend check, frontend build, and AI service check.

### Scenario: Review pipeline stages

- GIVEN the repository contains a Jenkinsfile
- WHEN a maintainer opens the pipeline definition
- THEN the required four stages SHALL be present with syntax-valid placeholders
